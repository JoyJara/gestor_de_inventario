{
  "name": "gestor_de_inventario",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "@types/express-session": "^1.18.1",
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "express-session": "^1.18.1"
  }
}
